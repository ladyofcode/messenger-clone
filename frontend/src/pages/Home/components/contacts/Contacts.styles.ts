import styled from "styled-components";

const Container = styled.ul`
  margin: 0;
  padding: 0;
  list-style: none;
`;

const ContactGroupTitle = styled.h2`
  color: #030363;
  font-size: 1em;
  margin-bottom: 0.5em;
`;

export const Styled = { 
  Container, 
  ContactGroupTitle 
};
