import styled from "styled-components";

const Icon = styled.img`
  display: flex;
  justify-content: center;
  align-items: center;

  &.primary {
    width: 32px;
    height: 32px;
  }
`;

export { Icon };
