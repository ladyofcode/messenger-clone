import styled from "styled-components";

const ChatLine = styled.div`
    margin-bottom: 20px;

    p:first-of-type {
        color: #3a3a3a;
        font-size: 1em;
    }

    p:last-of-type {
        color: #222222;
        margin-left: 15px;
    }
`;

export const Styled = {
    ChatLine,
};
