const LOCALSTORAGE_PREFIX = "MSN_CLONE_";

const constants = {
  LOGIN_SETTINGS: LOCALSTORAGE_PREFIX + "LOGIN_SETTINGS",
  BASE_PATH: "http://localhost:4000",
  SOCKETS_PATH: "http://localhost:30001",
};

export { constants };
